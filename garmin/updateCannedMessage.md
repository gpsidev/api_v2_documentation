What happens when a message is updated, you speficy the message id and the new text. and this process expires that message_id and creates a new one with the new text.

Example Response:

    {
    head: { ... },
    data: [
    {   id: 502997,
        result: "Saved"   
    }
    ]}
